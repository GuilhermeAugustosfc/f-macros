export type Ativo = {
  ativo_id: number;
  ativo_name: string;
  plate: string;
  description: string;
  client_id: number;
};

export type Client = {
  client_id: number;
  client_description: string;
};
